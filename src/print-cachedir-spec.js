'use strict'

/* eslint-env mocha */
const printCachedir = require('.')

describe('print-cachedir', () => {
  it('write this test', () => {
    console.assert(printCachedir, 'should export something')
  })
})
